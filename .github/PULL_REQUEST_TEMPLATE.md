## What does this change?

<!-- One or two sentences. -->

## Why?

<!-- Bug fixed, feature added, or debt paid down. Link an issue if there is one. -->

## Checklist

- [ ] `make test` passes (`go test -race -cover ./...`)
- [ ] `make check-templates` passes if `pkg/runtime` or a base/feature template changed
- [ ] `gofmt -l .` is clean
- [ ] Docs (`README.md`, `CHANGELOG.md`, etc.) updated if behavior or flags changed
